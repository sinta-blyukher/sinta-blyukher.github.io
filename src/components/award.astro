---
import '../styles/global.css'

const awards = [
    {name: 'P2HACKS' award:'Cyber Agent賞'},
];


---

<section>
    awards.map((nameDate) => (
    <ul>
        {
            nameDate.award.map(city) => 
        }
    </ul>
    ))
</section>